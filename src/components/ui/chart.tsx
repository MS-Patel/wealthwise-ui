/**
 * Chart wrapper placeholder.
 * The shadcn chart component had a Recharts type mismatch and is unused in
 * phase 1. Reintroduce when wiring up portfolio analytics — for now consume
 * Recharts primitives directly: import { ResponsiveContainer, ... } from "recharts".
 */
export {};
